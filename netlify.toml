[build]
  command = "npm run build" # Or "yarn build"
  publish = "dist"

[functions]
  directory = "netlify/functions" # If you're using Netlify Functions

[dev]
  command = "npm run dev" # Vite's dev server
  port = 3000
  publish = "public" #  If you have static assets outside 'dist'